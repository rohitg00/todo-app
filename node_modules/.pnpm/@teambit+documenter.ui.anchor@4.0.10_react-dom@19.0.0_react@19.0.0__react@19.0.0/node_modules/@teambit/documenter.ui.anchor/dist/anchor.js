import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import { Icon } from '@teambit/evangelist.elements.icon';
import styles from './anchor.module.scss';
/**
 * An anchor component with an icon for linking your docs
 */
export function Anchor({ href, className, ...restProps }) {
    const hashedHref = href && `#${href}`;
    return (_jsx("a", { id: href, href: hashedHref, className: classNames(className, styles.anchor), ...restProps, children: _jsx(Icon, { of: "anchor", className: styles.anchorIcon }) }));
}
//# sourceMappingURL=anchor.js.map