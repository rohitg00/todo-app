var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useEffect } from 'react';
import { Tab } from '@teambit/ui-foundation.ui.use-box.tab';
import { useTabValue } from './use-tab-value';
// @ts-ignore
import styles from './styles.module.scss';
export const ContentTabs = (_a) => {
    var { tabsContent, tabsUrlParam } = _a, rest = __rest(_a, ["tabsContent", "tabsUrlParam"]);
    const { activeTab, setActiveTab } = useTabValue(tabsUrlParam);
    const triggerResize = () => {
        const resizeEvent = new Event('resize');
        window.dispatchEvent(resizeEvent);
        if (document.body.dispatchEvent) {
            document.body.dispatchEvent(resizeEvent);
        }
    };
    useEffect(() => {
        const timeoutId = setTimeout(() => {
            triggerResize();
        }, 0);
        return () => clearTimeout(timeoutId);
    }, [activeTab]);
    return (React.createElement("div", Object.assign({}, rest),
        React.createElement("div", { className: styles.tabs }, tabsContent.map(({ title }, index) => {
            return (React.createElement(Tab, { key: index, isActive: index === activeTab, onClick: () => setActiveTab(index) }, title));
        })),
        tabsContent[activeTab].body));
};
//# sourceMappingURL=content-tabs.js.map