"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.JestTask = void 0;
const defender_tester_task_1 = require("@teambit/defender.tester-task");
const jest_tester_1 = require("./jest-tester");
const DEFAULT_NAME = 'JestTest';
const DEFAULT_DESCRIPTION = 'testing components using Jest';
/**
 * a task for a jest task.
 */
exports.JestTask = {
    from: (options) => {
        const name = options.name || DEFAULT_NAME;
        const description = options.description || DEFAULT_DESCRIPTION;
        return defender_tester_task_1.TesterTask.from(Object.assign(Object.assign({}, options), { name,
            description, tester: jest_tester_1.JestTester.from(options) }));
    },
    create: (options, { logger, worker, devFiles, }) => {
        const name = options.name || DEFAULT_NAME;
        const description = options.description || DEFAULT_DESCRIPTION;
        const tester = jest_tester_1.JestTester.create(options, { logger, worker });
        return defender_tester_task_1.TesterTask.create(Object.assign(Object.assign({}, options), { name, description }), { devFiles, tester });
    },
};
//# sourceMappingURL=jest-task.js.map