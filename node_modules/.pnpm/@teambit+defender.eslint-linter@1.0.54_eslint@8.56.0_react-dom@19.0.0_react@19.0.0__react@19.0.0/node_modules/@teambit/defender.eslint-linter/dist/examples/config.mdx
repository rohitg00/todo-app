This specific config extends the default ESLInt config of the React environment.

```js
module.exports = {
  /* extend the default ESLint config of the base env (in this example, react-env) */
  extends: [require.resolve('@teambit/react.react-env/config/eslintrc')],
  rules: {},
};
```
