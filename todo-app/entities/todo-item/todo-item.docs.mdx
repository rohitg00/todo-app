---
labels: ['todo item', 'entity']
description: 'A TodoItem entity.'
---

A component for a TodoItem entity, supports serialization and de-serialization of TodoItem objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const todoItem = TodoItem.from({
  name: 'hello'
});
```
